# 家計簿OS v1 再構築計画

## 方針
このリポジトリは試作段階で本番データを保持していないため、既存の試作コードを温存するより、拡張しやすい構造を優先して再構築する。

## アーキテクチャ
- `index.html`: 入口のみ
- `css/`: 共通スタイル
- `js/core/`: 認証、Supabase、状態、共通ユーティリティ
- `js/components/`: 共通UI
- `js/features/`: 機能単位
- DB: 家計データを正規化し、ユーザーIDでRLSを徹底

## 実装順
1. 基盤・認証
2. 家計設定（カテゴリ、摘要、決済方法、カード、口座）
3. 取引入力
4. 履歴・編集・削除
5. カード請求
6. 予算・月次
7. 資産・負債
8. ホームダッシュボード
9. テスト・UI改善

## 重要ルール
- 実際の家計データはまだないため、旧試作コードは設計参考として扱う。
- 機能追加は `features` 単位で行い、coreへの依存を最小化する。
- UIから直接Supabaseを叩かず、データアクセス層を経由する。
- カード利用額、請求額、口座残高、資産・負債を混同しない。
- 「記録した日」と「実際の取引日」を分離する。
- 将来MY-OSからリンクする独立アプリとして設計する。
