import { layout } from '../components/layout.js';
import { currentMonth,toMonth,yen,esc } from '../core/utils.js';
export function renderHome(s){const m=toMonth(s.settings?.target_year_month)||currentMonth();const rows=s.transactions.filter(t=>toMonth(t.transaction_date)===m);const income=rows.filter(t=>['income','borrowing'].includes(t.process_type)).reduce((a,t)=>a+Number(t.amount||0),0);const expense=rows.filter(t=>!['income','borrowing','repayment','transfer','card_payment'].includes(t.process_type)).reduce((a,t)=>a+Number(t.actual_payment??Math.max(0,Number(t.amount||0)-Number(t.points_used||0))),0);const repay=rows.filter(t=>t.process_type==='repayment').reduce((a,t)=>a+Number(t.amount||0),0);return layout('💰 家計簿OS','home',`<section class="card"><div class="between"><div><div class="muted small">対象年月</div><h2>${esc(m)}</h2></div><button class="primary" data-page="input">＋ 記録</button></div><div class="stats"><div class="stat"><span class="stat-label">収入・借入</span><span class="stat-value">${yen(income)}</span></div><div class="stat"><span class="stat-label">実支出</span><span class="stat-value">${yen(expense)}</span></div><div class="stat"><span class="stat-label">返済</span><span class="stat-value">${yen(repay)}</span></div></div><div class="stat" style="margin-top:10px"><span class="stat-label">実収支</span><span class="stat-value">${yen(income-expense-repay)}</span></div></section><section class="card"><h3>最近の記録</h3>${rows.slice(0,8).map(t=>`<div class="list-item"><div class="between"><b>${esc(t.description)}</b><b>${yen(t.amount)}</b></div><div class="muted small">${esc(t.transaction_date)} ・ ${esc(t.category_name||'その他')} ・ ${esc(t.payment_method_name||'')}</div></div>`).join('')||'<p class="muted">まだ記録がありません。</p>'}</section>`)}
