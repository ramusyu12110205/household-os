import { layout } from '../../components/layout.js';
import { yen,esc } from '../../core/utils.js';
export function renderAssets(s){const balance=s.accounts.reduce((a,x)=>a+Number(x.actual_balance??x.initial_balance??0),0),cards=s.cards.reduce((a,x)=>a+Number(x.actual_balance||0),0);return layout('💰 家計簿OS','assets',`<div class="grid"><section class="card"><h2>🏦 口座・現金</h2><div class="stat"><span class="stat-label">実残高合計</span><span class="stat-value">${yen(balance)}</span></div>${s.accounts.map(a=>`<div class="list-item"><div class="between"><b>${esc(a.name)}</b><b>${yen(a.actual_balance??a.initial_balance)}</b></div><div class="muted small">${esc(a.account_type||'')}</div></div>`).join('')||'<p class="muted">口座なし</p>'}</section><section class="card"><h2>💳 カード</h2><div class="stat"><span class="stat-label">カード残高合計</span><span class="stat-value">${yen(cards)}</span></div>${s.cards.map(c=>`<div class="list-item"><div class="between"><b>${esc(c.name)}</b><b>${yen(c.actual_balance)}</b></div><div class="muted small">締め ${c.close_day}日 / 引落 ${c.withdrawal_day}日</div></div>`).join('')||'<p class="muted">カードなし</p>'}</section></div>`)}
