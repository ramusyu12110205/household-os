import { supabase } from '../core/supabase.js';
export function renderLogin(){document.getElementById('app').innerHTML=`<div class="container auth"><section class="card authbox"><div style="font-size:48px">💰</div><h1>家計簿OS</h1><p class="muted">家計をシンプルに管理</p><button class="primary" style="width:100%" id="login">Googleでログイン</button><p id="auth-error" class="small"></p></section></div>`;document.getElementById('login').onclick=async()=>{const{error}=await supabase.auth.signInWithOAuth({provider:'google',options:{redirectTo:location.href}});if(error)document.getElementById('auth-error').textContent=error.message}}
