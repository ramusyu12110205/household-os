import { layout } from '../../components/layout.js';
import { esc } from '../../core/utils.js';
export function renderSettings(s){return layout('💰 家計簿OS','settings',`<section class="card"><h2>⚙ 家計設定</h2><p class="muted">ここを基準に、摘要・決済方法・カード・口座・カテゴリ・予算を管理します。</p><div class="grid"><div><h3>摘要</h3>${s.summaries.map(x=>`<div class="list-item">${esc(x.name)} <span class="muted small">${esc(x.category_name||'')} / ${esc(x.process_type||'')}</span></div>`).join('')||'<p class="muted">未登録</p>'}</div><div><h3>決済方法</h3>${s.payments.map(x=>`<div class="list-item">${esc(x.name)}</div>`).join('')||'<p class="muted">未登録</p>'}</div><div><h3>カード</h3>${s.cards.map(x=>`<div class="list-item">${esc(x.name)} <span class="muted small">締め${x.close_day}日・引落${x.withdrawal_day}日</span></div>`).join('')||'<p class="muted">未登録</p>'}</div><div><h3>口座</h3>${s.accounts.map(x=>`<div class="list-item">${esc(x.name)}</div>`).join('')||'<p class="muted">未登録</p>'}</div></div></section>`)}
