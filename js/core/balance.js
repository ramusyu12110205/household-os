import { actualAmount } from './transactionRules.js';
export function calculateBalances(state){const accounts=new Map(state.accounts.map(a=>[a.id,{...a,balance:Number(a.initial_balance||0)}]));const cards=new Map(state.cards.map(c=>[c.id,{...c,balance:0}]));for(const t of [...state.transactions].sort((a,b)=>String(a.transaction_date).localeCompare(String(b.transaction_date)))){const n=actualAmount(t.amount,t.points_used);switch(t.process_type){case'normal':if(t.account_id&&accounts.has(t.account_id))accounts.get(t.account_id).balance-=n;if(t.billing_card_id&&cards.has(t.billing_card_id))cards.get(t.billing_card_id).balance+=n;break;case'income':case'borrowing':if(t.account_id&&accounts.has(t.account_id))accounts.get(t.account_id).balance+=n;break;case'transfer':if(t.from_account_id&&accounts.has(t.from_account_id))accounts.get(t.from_account_id).balance-=n;if(t.to_account_id&&accounts.has(t.to_account_id))accounts.get(t.to_account_id).balance+=n;break;case'repayment':case'card_payment':if(t.account_id&&accounts.has(t.account_id))accounts.get(t.account_id).balance-=n;if(t.target_card_id&&cards.has(t.target_card_id))cards.get(t.target_card_id).balance-=n;break}}return{accounts:[...accounts.values()],cards:[...cards.values()]}}
